<h1>一覧表示</h1>
<%= "投稿は全部で#{@pictures.count}個です。" %>

<% @pictures.each do |picture| %>
  <div>
    <h4>
      <%= link_to "#{picture.content}", picture_path(picture) %>
    </h4>
    <p>
      <%= "投稿者：#{picture.user.name}" %>
    </p>
    <p>
      <%= "投稿日：#{picture.created_at.to_s.slice(0..18)}" %>
    </p>
  </div>
<% end %>

<br>
<%= link_to "投稿する", new_picture_path %>